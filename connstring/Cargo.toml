[package]
name = "wosh-connstring"
version = "0.1.0"
edition = "2021"
description = "The wosh pairing connection string: iroh pubkey + relay URL + optional token, as one base64url blob."
publish = false

[dependencies]
base64 = { version = "0.22", default-features = false, features = ["alloc"] }
# The pairing proof (`pairing_proof`) and its comparison (`proof_eq`).
# All three are already in this workspace's tree at these versions, and
# all are no_std, so they cost the wasm components nothing.
subtle = { version = "2.6", default-features = false }
hmac = { version = "0.12", default-features = false }
sha2 = { version = "0.10", default-features = false }
postcard = { version = "1", default-features = false, features = ["alloc"] }
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }

[lib]
name = "wosh_connstring"
path = "src/lib.rs"
