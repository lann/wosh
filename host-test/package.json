{
  "name": "experiment-mosh-host-test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "M1 conformance harness: the engine component (jco-transpiled, wasi served by preview2-shim) driven from node against real mosh servers over loopback UDP.",
  "scripts": {
    "transpile": "node jco-transpile.mjs transpile ../engine-go/main.wasm --name mosh-engine -o generated",
    "conformance-c": "node run-conformance.mjs --server c",
    "conformance-go": "node run-conformance.mjs --server go"
  },
  "devDependencies": {
    "@bytecodealliance/jco-transpile": "file:../../polymorph-iroh/.deps/jco/packages/jco-transpile"
  },
  "dependencies": {
    "@bytecodealliance/preview2-shim": "^0.19.0"
  }
}
