{
  "name": "wosh-host-test",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Harness support for the node-run gate scripts (browser-smoke, web-tests, browser-e2e): playwright + the ws dev bridge. Component hosting itself is deltic on the Deno lane (deltic-host.ts; root deno.json).",
  "scripts": {
    "web-tests": "node web-tests.mjs"
  },
  "dependencies": {
    "playwright-core": "^1.62.1",
    "ws": "^8.21.3"
  }
}
